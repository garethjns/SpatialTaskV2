function stats = fitGLMLinearPP(allData)

% For the 5? positions (folded) fit model

poss = unique(abs(allData.Position));
nPoss = numel(poss);

for p = 1:nPoss
   
    % Wait does this make sense?
    
    pIdx = allData.
    data = allData
    
end